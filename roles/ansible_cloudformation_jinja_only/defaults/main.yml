---
ec2_key: "{{ lookup('aws_ssm', '/config/CKAN/ec2KeyPair', region=region) }}"

common_stack:
  state: "{{ state | default('present')}}"
  region: "{{ region | default('ap-southeast-2')}}"
  disable_rollback: true
  template_jinja: {}
  template: {}
  template_parameters: {}
  tags: {}
