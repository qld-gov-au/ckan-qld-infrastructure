---
cloudformation_stacks:
  - name: "CKAN-CLOUDFRONT-LOGGING-BUCKET"
    state: present
    region: "{{ region }}"
    disable_rollback: 'true'
    template: "templates/cloudfront-logging-bucket.cfn.yml"
    template_parameters:
      LogBucketName: "{{ global_ssm_vars['/config/CKAN/s3LogsBucket'] }}"
    tags:
      Service: "CKAN"
      Division: "{{ Division }}"
      Owner: "{{ Owner }}"
      Version: "1.0"
